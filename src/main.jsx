import React from "react";
import ReactDOM from "react-dom/client";
import BlizzardApp from "./BlizzardApp";

ReactDOM.createRoot(document.getElementById("root")).render(
	<React.StrictMode>
		<BlizzardApp />
	</React.StrictMode>
);
